/**
 * git hook 发出的请求
 * @author Philip
 */
const Proxy = require('../services/proxy')

/**
 * @git提交钩子
 * @param {object} 已编译的请求数据
 * @param {string} 日志 id
 * @controller
 */
module.exports.githook = (parsed, logId, req, res) => {
    
}
