/**
 * 接口测试
 * @author Philip
 */
const request = require("supertest")

describe('log', () => {
  it('should get log list', () => {
    assert.equal([1,2,3].indexOf(4), -1);
  });
  
  it('should get log list', () => {
    assert.equal([1,2,3].indexOf(4), -1);
  });
});

describe('user', () => {
  it('should get log list', () => {
    assert.equal([1,2,3].indexOf(4), -1);
  });
  
  it('should get log list', () => {
    assert.equal([1,2,3].indexOf(4), -1);
  });
});
